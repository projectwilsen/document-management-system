[project]
name = "document-management-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "customtkinter>=5.2",
    "pdfplumber>=0.11",
    "requests>=2.32",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
